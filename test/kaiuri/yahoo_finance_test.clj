(ns kaiuri.yahoo-finance-test
  (:require [clojure.test :refer :all]
            [kaiuri.clj-yahoo-finance.core :as yf]))

;; TODO: write tests
(deftest a-test
  (testing "WRITE SOME TESTS, DUDE"
    (is (= 0 0))))
